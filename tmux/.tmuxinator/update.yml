name: update
root: ~/.ghq/github.com/iyuuya/bksd

windows:
  - bksd: git fetch --all && git pull && exit
  - anyenv: anyenv update && exit
  - brew: brew update && brew upgrade --all && brew cleanup && exit
